import { MatPaginationStyleDirective } from './mat-pagination-style.directive';

describe('MatPaginationStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new MatPaginationStyleDirective();
    expect(directive).toBeTruthy();
  });
});
